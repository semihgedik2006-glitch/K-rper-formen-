__pycache__/
*.pyc
functions/.env
functions/node_modules/

# Bildschirmfotos aus den Testlaeufen – entstehen bei jedem Lauf neu
tests/*.png
# Bericht des Lasttests – ebenfalls bei jedem Lauf neu, sonst rauscht
# jeder Durchlauf als Aenderung durch den Verlauf
tests/last-bericht.txt

# Abhaengigkeiten der Regeltests
tests/rules/node_modules
tests/rules/package-lock.json
firestore-debug.log
# Derselbe Emulator schreibt eine zweite Datei. Die stand hier nicht und
# tauchte deshalb nach jedem Regellauf als unversionierte Aenderung auf.
firebase-debug.log

# Mess-Skripte und Protokolle einer Sitzung. Entstehen beim Nachmessen,
# gehoeren nicht ins Repo — sie sind Werkzeug, kein Ergebnis.
scratchpad/
