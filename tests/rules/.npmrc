# Feste Versionen, ohne dass jemand daran denken muss.
#
# Ohne diese Zeile schreibt "npm install firebase-tools@14.27.0" den
# Eintrag als "^14.27.0" zurueck — und hebt damit genau die Sperre auf,
# die hier stehen soll. Passiert am 17.8. beim ersten Nachziehen sofort.
#
# Warum die Sperre ueberhaupt: siehe README.md nebenan. Kurz — oertlich
# lag firebase-tools 15.26.0, die Auslieferung nahm 14.27.0, und die
# beiden Emulatoren waren sich ueber einen Regelpfad uneinig. Eine
# Pruefung, die woanders laeuft als die Auslieferung, prueft nichts.
save-exact=true
